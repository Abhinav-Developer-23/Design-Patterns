package org.example;

/**
 * Shape interface - Product interface in Factory Pattern
 * This interface defines the contract that all concrete shapes must implement
 */
public interface Shape {
    /**
     * Method to draw the shape
     */
    void draw();
}
